!!!
%html
  %head
    %meta(charset="UTF-8")
    %title= "#{page_title}"
    %meta(name="viewport" content="width=device-width" initial-scale="1.0")
    - if url_for(only_path: true) == "/"
      %meta(name="description" content="groonga-devの本文を検索できるサイト。キーワードの前後を部分表示します。fromなどでの絞り込み機能付き。全文検索エンジンとしてGroongaを使用。")
      %meta(name="keywords" content="groonga-dev,検索,全文検索")
    - else
      %meta(name="robots" content="noindex")
    %link(href="#{@sub_url}css/groonga_dev_search.css" rel="stylesheet" media="screen")
    %link(rel="shortcut icon" type="image/vnd.microsoft.icon" href="favicon.ico")
  %body
    = render :_header, layout: false

    .container
      = yield

    #footer.container.text-right
      = render :_footer, layout: false

    %script(src="https://unpkg.com/hyperapp")
    %script(src="groonga_dev_search.js")
